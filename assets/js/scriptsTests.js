import scripts from './scripts'
describe('scripts', () => {
    var options
    beforeEach(() => {
        options = {}
    })
    describe('submitPaymentRequest', () => {
        it('does stuffs', () => {
            scripts //?
        })
    })
})
